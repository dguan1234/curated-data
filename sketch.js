function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  
  ellipse(100,100,100)
  ellipse(300,100,100)
  textSize(32);
  textAlign(CENTER);
  text('curated data', 200, 200);
  
  
}